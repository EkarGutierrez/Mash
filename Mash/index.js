import React from "react";
import { ReactDOM } from "react";
import App from './App';
import 'bootstrap/dist/css/bootstrap.mi'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.reder(
    <React.StrictMode>
        <App/>
    </React.StrictMode>
);